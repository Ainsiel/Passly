apiVersion: 1

providers:
  - name: passly
    orgId: 1
    folder: Passly
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
